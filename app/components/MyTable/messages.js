/*
 * MyTable Messages
 *
 * This contains all the text for the MyTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MyTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MyTable component!',
  },
});
