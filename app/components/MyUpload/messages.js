/*
 * MyUpload Messages
 *
 * This contains all the text for the MyUpload component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MyUpload';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MyUpload component!',
  },
});
