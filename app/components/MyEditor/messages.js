/*
 * MyEditor Messages
 *
 * This contains all the text for the MyEditor component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MyEditor';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MyEditor component!',
  },
});
