/*
 * AdminTable Messages
 *
 * This contains all the text for the AdminTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AdminTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminTable component!',
  },
});
