/*
 * TempCom Messages
 *
 * This contains all the text for the TempCom component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TempCom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TempCom component!',
  },
});
