/*
 * TitleCom Messages
 *
 * This contains all the text for the TitleCom component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TitleCom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TitleCom component!',
  },
});
