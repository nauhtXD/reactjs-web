/*
 * WeatherHistory Messages
 *
 * This contains all the text for the WeatherHistory component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WeatherHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WeatherHistory component!',
  },
});
