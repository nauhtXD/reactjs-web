import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Garden';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Garden container!',
  },
});
