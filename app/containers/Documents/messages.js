import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Documents';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Documents container!',
  },
});
