import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WeatherMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WeatherMap container!',
  },
});
