import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Forum';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Forum container!',
  },
});
