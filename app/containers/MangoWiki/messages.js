import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MangoWiki';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MangoWiki container!',
  },
});
